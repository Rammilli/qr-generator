// Render backend URL — update this when your Render service URL changes
// Locally: set VITE_API_URL in frontend/.env to override
export const API_URL =
  import.meta.env.VITE_API_URL || "https://qr-generator-sxwa.onrender.com";